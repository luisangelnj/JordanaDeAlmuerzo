import { Router } from 'express';
// import { getOrderStatus } from '../controllers/kitchen.status.controller';

const router = Router();

// router.post('/status', getOrderStatus);

export default router;